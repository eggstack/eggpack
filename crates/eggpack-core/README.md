# eggpack-core

Producer-side planning and finalized-manifest construction. This crate reads only explicitly named finalized files for manifest evidence. PackConfig/ReleasePlan planning is pure and does not execute builds. It does not publish, acquire, install, or select releases.
